<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.exoplatform.kernel</groupId>
  <artifactId>config</artifactId>
  <name>eXo Kernel</name>
  <version>trunk</version>
  <packaging>pom</packaging>

  <modules>
    <module>container</module>
    <module>commons</module>

    <module>component/common</module>
    <module>component/remote</module>
    <module>component/cache</module>
    <module>component/command</module>
  </modules>

  <build>
    <resources>
      <resource>
        <directory>src/main/java</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.xml</include>
          <include>**/*.drl</include>
        </includes>
      </resource>
    </resources>
    <testResources>
      <testResource>
        <directory>src/test/java</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.xml</include>
          <include>**/*.drl</include>
        </includes>
      </testResource>
    </testResources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration> <source>1.5</source><target>1.5</target><optimize>true</optimize></configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${exo.test.skip}</skip>
		  <useFile>${exo.test.use.file}</useFile>
		  <forkMode>always</forkMode>
          <testFailureIgnore>${exo.test.failure.ignore}</testFailureIgnore>
          <includes>
            <include implementation="java.lang.String">**/${test.classes}*.java</include>
          </includes>
                    <excludes>
		              <exclude>**/*$*</exclude>
		              <exclude>**/SchedulerServiceTestBase.java</exclude>
		              <exclude>**/TestCompressData.java</exclude><!-- need to add test resources -->
		              <exclude>**/LogTest.java</exclude><!-- need to review test logic -->
		  			<exclude>**/TestSimpleThreadPoolService.java</exclude><!-- no such service in *config*.xml -->
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>default</id>
      <activation>
        <property><name>default</name></property>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <exo.platform.version>trunk</exo.platform.version>
        <org.exoplatform.tool.version>trunk</org.exoplatform.tool.version>
        <org.exoplatform.kernel.version>trunk</org.exoplatform.kernel.version>
        <test.classes>Test</test.classes>
        <test.skip>true</test.skip>
      </properties>
    </profile>
  </profiles>
</project>
