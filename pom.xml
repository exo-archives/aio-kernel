<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.exoplatform</groupId>
    <artifactId>parent</artifactId>
    <version>1.1.1</version>
  </parent>

  <groupId>org.exoplatform.kernel</groupId>
  <artifactId>kernel-parent</artifactId>
  <name>eXo Kernel</name>
  <version>2.0.9-SNAPSHOT</version>
  <packaging>pom</packaging>
  <properties>
     <exo.product.name>exo-kernel</exo.product.name>
     <exo.product.specification>2.0.7</exo.product.specification>
  </properties>
  <scm>
    <connection>scm:svn:http://svn.exoplatform.org/svnroot/exoplatform/projects/kernel/branches/2.0.x</connection>
    <developerConnection>scm:svn:http://svn.exoplatform.org/svnroot/exoplatform/projects/kernel/branches/2.0.x</developerConnection>
    <url>http://fisheye.exoplatform.org/browse/projects/kernel/branches/2.0.x</url>
  </scm>

  <modules>
    <module>container</module>
    <module>commons</module>
    <module>component/common</module>
    <module>component/remote</module>
    <module>component/cache</module>
    <module>component/ext/cache/impl/jboss/v1</module>
    <module>component/ext/cache/impl/jboss/v3</module>
    <module>component/command</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>emma</groupId>
        <artifactId>emma</artifactId>
        <version>2.0.5312</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>jgroups</groupId>
        <artifactId>jgroups</artifactId>
        <version>2.5.2</version>
        <scope>compile</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>emma</groupId>
      <artifactId>emma</artifactId>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/java</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.xml</include>
          <include>**/*.drl</include>
        </includes>
      </resource>
    </resources>
    <testResources>
      <testResource>
        <directory>src/test/java</directory>
        <includes>
          <include>**/*.properties</include>
          <include>**/*.xml</include>
          <include>**/*.drl</include>
        </includes>
      </testResource>
    </testResources>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skip>${exo.test.skip}</skip>
          <useFile>${exo.test.use.file}</useFile>
          <forkMode>always</forkMode>
          <testFailureIgnore>${exo.test.failure.ignore}</testFailureIgnore>
          <classesDirectory>${exo.test.classesdirectory}</classesDirectory>
          <includes>
            <include>**/${exo.test.includes}.java</include>
          </includes>
          <excludes>
            <exclude>**/*$*</exclude>
            <exclude>**/SchedulerServiceTestBase.java</exclude>
            <exclude>**/TestCompressData.java</exclude><!-- need to add test resources -->
            <exclude>**/LogTest.java</exclude><!-- need to review test logic -->
            <exclude>**/TestSimpleThreadPoolService.java</exclude><!-- no such service in *config*.xml -->
            <exclude>**/TestCommunicationService.java</exclude><!--need to tune firewall-->
            <exclude>**/TestNetService.java</exclude><!--need to tune firewall-->
          </excludes>
          <systemProperties>
            <property>
              <name>emma.coverage.out.file</name>
              <value>target/emma/coverage.ec</value>
            </property>
          </systemProperties>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.3</version>
        <executions>
          <execution>
            <id>emma1</id>
            <phase>test-compile</phase>
            <configuration>
              <tasks if="emma.run">
                <echo message="[EMMA] Start classes instrumentation ..." />
                <taskdef resource="emma_ant.properties" classpathref="maven.dependency.classpath" />
                <mkdir dir="target" />
                <mkdir dir="target/classes" />
                <mkdir dir="target/emma/classes" />
                <!-- Do nothing if the directory already exist-->
                <copy todir="target/emma/classes" verbose="false" overwrite="true">
                  <fileset dir="target/classes" />
                </copy>
                <emma enabled="true">
                  <instr mode="overwrite" metadatafile="target/emma/metadata.em">
                    <filter excludes="*.xml.*" />
                    <filter excludes="*JiBX*" />
                    <instrpath>
                      <dirset dir="target">
                        <include name="emma/classes" />
                      </dirset>
                    </instrpath>
                  </instr>
                </emma>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
          <execution>
            <id>emma2</id>
            <phase>test</phase>
            <configuration>
              <tasks if="emma.run">
                <echo message="[EMMA] Start report generation..." />
                <taskdef resource="emma_ant.properties" classpathref="maven.dependency.classpath" />
                <emma enabled="true">
                  <!-- merged file will be rewritted by last project -->
                  <merge outfile="${exo.projects.directory.src}/kernel/branches/2.0.x/target/coverage.es">
                    <fileset dir="${exo.projects.directory.src}/kernel/branches/2.0.x/">
                      <include name="**/target/emma/*.e*" />
                      <include name="**/target/emma/*.e*" />
                    </fileset>
                    <!-- just for the first launnch -->
                    <fileset file="${exo.projects.directory.config}/coverage.es" />
                  </merge>
                  <report>
                    <sourcepath>
                      <dirset dir="${exo.projects.directory.src}/kernel/tags/">
                        <include name="**/src/**/java" />
                      </dirset>
                    </sourcepath>
                    <fileset dir="${exo.projects.directory.src}/kernel/branches/2.0.x/target/">
                      <include name="*.es" />
                    </fileset>
                    <html outfile="${exo.projects.directory.src}/kernel/branches/2.0.x/target/coverage.html" />
                    <xml outfile="${exo.projects.directory.src}/kernel/branches/2.0.x/target/coverage.xml" />
                  </report>
                </emma>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>emma</groupId>
            <artifactId>emma</artifactId>
            <version>2.0.5312</version>
            <scope>test</scope>
          </dependency>
          <dependency>
            <groupId>emma</groupId>
            <artifactId>emma_ant</artifactId>
            <version>2.0.5312</version>
            <scope>test</scope>
          </dependency>
        </dependencies>
      </plugin>
    </plugins>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <configuration>
          <configLocation>${exo.projects.directory.config}/exo-checkstyle-checker.xml</configLocation>
          <suppressionsLocation>${exo.projects.directory.config}/exo-checkstyle-suppressions.xml</suppressionsLocation>
        </configuration>
      </plugin>
    </plugins>
  </reporting>

</project>
